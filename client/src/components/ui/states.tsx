import type { ReactNode } from 'react';
import { AlertCircle, Inbox } from 'lucide-react';
import { Button } from './button';

export function TableSkeleton({ rows = 6 }: { rows?: number }) { return <div className="animate-pulse p-5"><div className="mb-5 h-4 w-1/3 rounded bg-slate-100" />{Array.from({ length: rows }, (_, index) => <div className="mb-4 flex gap-5" key={index}><div className="h-8 flex-1 rounded bg-slate-100" /><div className="h-8 flex-1 rounded bg-slate-100" /><div className="h-8 flex-1 rounded bg-slate-100" /></div>)}</div>; }
export function CardSkeletons({ count = 4 }: { count?: number }) { return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{Array.from({ length: count }, (_, index) => <div key={index} className="surface h-32 animate-pulse bg-slate-100" />)}</div>; }
export function EmptyState({ title, description, action }: { title: string; description: string; action?: ReactNode }) { return <div className="flex min-h-64 flex-col items-center justify-center px-6 py-12 text-center"><div className="mb-4 rounded-full bg-slate-100 p-3 text-slate-500"><Inbox className="h-6 w-6" /></div><h3 className="text-base font-semibold text-ink">{title}</h3><p className="mt-1 max-w-sm text-sm leading-6 text-slate-500">{description}</p>{action && <div className="mt-5">{action}</div>}</div>; }
export function ErrorState({ message = "We couldn't load this information.", retry }: { message?: string; retry?: () => void }) { return <div className="flex min-h-64 flex-col items-center justify-center px-6 py-12 text-center"><div className="mb-4 rounded-full bg-red-50 p-3 text-red-600"><AlertCircle className="h-6 w-6" /></div><h3 className="text-base font-semibold text-ink">Something went wrong</h3><p className="mt-1 max-w-sm text-sm text-slate-500">{message}</p>{retry && <Button variant="secondary" className="mt-5" onClick={retry}>Try again</Button>}</div>; }
